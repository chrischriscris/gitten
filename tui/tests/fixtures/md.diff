diff --git a/README.md b/README.md
--- a/README.md
+++ b/README.md
@@ -1,22 +1,24 @@
 # gitten
 
-A terminal diff view with **bold** claims and [a link](https://example.com/very/long/url/path).
+A terminal diff view with **bolder** claims and [a link](https://example.com/other).
 
-## Usage
+## Getting started
+
+Run the development script: *gently*.
     ./dev.sh diff        # rebuild on every save
 
-- first
+- first
   - nested, whose own text runs on long enough to wrap at any width a terminal is likely to open with
     and the continuation line the source carried under its bullet
 
 > quoted prose, with the bar drawn where the angle brackets were
-```
+```rust
 let table_flow = squeeze(&grid.widths, cols); // water-filling, not proportional, and the budget is per line rather than per diff
 ```
 
 ----
 
 | stage | detail |
 |-------|--------|
 | parse | the log, then assign the lanes, colour every branch, and fit the graph to the twelve the gutter can draw |
 | lanes | 301 ms |
diff --git a/src/draw.rs b/src/draw.rs
--- a/src/draw.rs
+++ b/src/draw.rs
@@ -1,3 +1,4 @@
 fn draw(row: &Row, pen: &mut Pen) {
-    pen.put("markdown", row.ink);
+    pen.put("markdown rows", row.ink);
+    pen.wash(row.background());
 }
